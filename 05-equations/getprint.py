#!/usr/bin/env python
import sys
import numpy

if (len(sys.argv) == 1):
	print("The first command line argument must be a file with human-readable equations.")
	print("E.g. \"input.txt\"")
	sys.exit()
